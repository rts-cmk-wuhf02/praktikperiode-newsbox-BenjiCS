module.exports = {
  theme: {
    extend: {
      colors: {
        // Primary Colors
        sage: 'var(--sage)',
        rust: 'var(--rust)',
        drab: 'var(--drab)',
        danger: 'var(--danger)',

        // Secondary Colors
        ice:'var(--ice)',
        fossil:'var(--fossil)',
        sand:'var(--sand)',

        // Typography Colors
        onyx: 'var(--onyx)',
        slate:'var(--slate)',
        snow:'var(--snow)',
      },
      fontFamily:{
        primary:["Roboto", "sans-serif"],
        alt:["Kosugi Maru", "sans-serif"]
      },
    },
  },
  variants: {},
  plugins: [],
}